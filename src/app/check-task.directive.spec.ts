import { CheckTaskDirective } from './check-task.directive';

describe('CheckTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckTaskDirective();
    expect(directive).toBeTruthy();
  });
});
